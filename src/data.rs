use std::LinkedList;

struct Data {
    users: LinkedList< User >,
    groups: LinkedList< Group >
}

impl Data {
    //! fn create_group(...);
    //! fn register_user(...);
    //! fn delete_group(...);
    //! fn delete_user(...);
}